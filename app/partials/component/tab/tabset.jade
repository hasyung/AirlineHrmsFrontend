div
    ul(class="nav nav-{{type || 'tabs'}}" ng-class="{'nav-stacked': vertical, 'nav-justified': justified}" ng-transclude)
    .tab-content
        .tab-pane(ng-repeat="tab in tabs" ng-class="{active: tab.active}" ng-if="tab.active" tab-content-transclude="tab")