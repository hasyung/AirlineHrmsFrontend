.modal.fade.modal-panel-wrapper(role="panel" ng-class="{in: animate}" ng-style="{'z-index': 1050 + index*10, display: 'block'}" ng-click="close($event)")
    .modal-panel(ng-class="{'modal-panel-sm': size =='sm', 'modal-panel-lg': size == 'lg'}")
        .modal-panel-content(modal-transclude)
