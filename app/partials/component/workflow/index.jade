block heading

tabset(flex)
    if user_list
        tab(heading="#{user_list}")
            include user_list
    tab(heading="待处理")
        include check_list
    tab(heading="历史记录")
        include history
