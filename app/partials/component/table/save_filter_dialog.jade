md-dialog.save_filter
    md-toolbar.md-accent(md-theme="hrms")
                .md-toolbar-tools
                    span 保存过滤器
    .dialog-content
        form.form-validator-dialog(name="filter_form" ng-submit="filter.saveFilter(filter_name) && dialog.close()")
            div(layout)
                md-input-container(flex)
                    label 过滤器名称
                    input(type="text" ng-model="filter_name" required)
            //- .form-group-row
            //-     label.form-control-label  过滤器名称：
            //-     input.form-control-input(type="text" required ng-model="filter_name")
            .form-group-row
                .pull-right.button-group
                    md-button.md-primary.md-raised(type="submit" ng-disabled="filter_form.$invalid") 提交