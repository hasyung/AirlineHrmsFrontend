md-dialog.personal_number(ng-controller="calcStepCtrl as stepCtrl" ng-init="stepCtrl.loadFromServer(dialog.category, dialog.month, dialog.employee_id)")
    md-toolbar.md-accent(md-theme="hrms")
            .md-toolbar-tools
                span 计算过程
    md-content.sticky-container
        .info-header
            span.name {{dialog.employee_name}}
            span.no {{dialog.employee_no}}
        .info-body
            ul
                li.tcell
                    .theader 步骤
                    .tbody {{}}

    .md-actions
        md-button(ng-click="dialog.close()") 关 闭
