span(flex)

span(ng-show="!ctrl.editing")
    md-button.md-primary.md-raised(ng-click="ctrl.editing=true") 编辑

span(ng-hide="!ctrl.editing")
    md-button.md-raised.white(ng-click="ctrl.resetDynamicConfig()") 取消
    md-button.md-primary.md-raised(ng-disabled="configForm.$invalid") 保存